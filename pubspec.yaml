name: booster_signups
description: An app to log signups for Bellarmine College Prep&#x27;s Booster Club

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  google_sign_in: ^4.1.1
  flutter_signin_button: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
